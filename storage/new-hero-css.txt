/* ===== Explore panel (replaces become-a-seller / become-a-courier) ===== */
.html-body .explore-panel {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  gap: 24px;
  height: 100%;
  width: 100%;
  padding: 24px;
  background-color: #faf2f1;
  border: 1px solid #e8e2e0;
  border-radius: 8px;
  box-shadow: 0px 10px 40px #2926260a;
}

.html-body .explore-panel__grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16px;
  flex: 1;
}

.html-body .explore-tile {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12px;
  padding: 24px 12px;
  background-color: #fff8f7;
  border: 1px solid #79545c;
  border-radius: 8px;
  transition: background-color 0.15s ease, transform 0.15s ease;
}

.html-body .explore-tile:hover {
  background-color: #f7d6d0;
  transform: translateY(-2px);
}

.html-body .explore-tile span {
  font-family: "Montserrat", Helvetica;
  font-weight: 600;
  font-size: 12px;
  letter-spacing: 0.6px;
  color: #1e1b1b;
  text-align: center;
}

.html-body .explore-panel__actions {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.html-body .explore-btn {
  all: unset;
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  padding: 14px 0;
  border-radius: 2px;
  font-family: "Montserrat", Helvetica;
  font-weight: 500;
  font-size: 13px;
  letter-spacing: 0.7px;
  text-align: center;
  cursor: pointer;
  transition: background-color 0.15s ease, color 0.15s ease;
}

.html-body .explore-btn--dark {
  background-color: #1e1b1b;
  color: #fff8f7;
}

.html-body .explore-btn--dark:hover {
  background-color: #33302f;
}

.html-body .explore-btn--outline {
  background-color: transparent;
  color: #79545c;
  border: 1px solid #79545c;
}

.html-body .explore-btn--outline:hover {
  background-color: #f7d6d0;
}

/* Icon wrapper still used by the product-card ADD button */
.html-body .div-3 {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  flex: 0 0 auto;
}

/* Tablet: panel keeps its 2x2 grid, just narrower */
@media (max-width: 1024px) {
  .html-body .right-ctas-stack {
    display: block; /* override the old auto-fit grid used for the two cards */
  }
}